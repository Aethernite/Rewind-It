pipeline {
    agent any
      
    stages {
        stage('Build') {
            steps {
			    dir("server") {
                   sh 'mvn -B -DskipTests clean package'
                }
            }
        }
        stage('Deliver') {
            steps {
                sh './server/Jenkins/scripts/deliver.sh'
            }
        }
    }
}